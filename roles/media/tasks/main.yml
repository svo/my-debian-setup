- name: Add jellyfin apt key
  ansible.builtin.apt_key:
    url: https://repo.jellyfin.org/jellyfin_team.gpg.key
    keyring: /etc/apt/trusted.gpg.d/jellyfin.gpg
  become: yes

- name: Install jellyfin apt repository
  ansible.builtin.apt_repository: repo='deb https://repo.jellyfin.org/{{ ansible_distribution | lower }} {{ ansible_distribution_release }} main' state=present
  become: yes

- name: Install jellyfin
  ansible.builtin.apt: name=jellyfin update_cache=yes
  become: yes

- name: Install commandline metadata tool
  ansible.builtin.apt: pkg=libimage-exiftool-perl
  become: yes

- name: Install VLC media player
  ansible.builtin.apt: pkg=vlc
  become: yes

- name: Add spotify apt key
  ansible.builtin.apt_key: url=https://download.spotify.com/debian/pubkey_C85668DF69375001.gpg
  become: yes

- name: Add spotify repository
  ansible.builtin.apt_repository: repo="deb http://repository.spotify.com stable non-free"
  become: yes

- name: Install spotify client
  ansible.builtin.apt: pkg=spotify-client update_cache=yes
  ignore_errors: yes
  become: yes

- name: Install GIMP photo editor
  ansible.builtin.apt: pkg=gimp
  become: yes

- name: Install handbrake video converter
  ansible.builtin.apt: pkg=handbrake
  become: yes

- name: Download OpenAudible .deb
  ansible.builtin.get_url: url=https://github.com/openaudible/openaudible/releases/download/v3.5.3/OpenAudible_3.5.3_x86_64.deb dest=~/Installs/OpenAudible_x86_64.deb

- name: Install OpenAudible
  ansible.builtin.apt: deb=/tmp/Installs/OpenAudible_x86_64.deb
  become: yes

- name: Install Cheese for webcam support
  ansible.builtin.apt: pkg=cheese
  become: yes

- name: Install capture program for webcam
  ansible.builtin.apt: pkg=motion
  become: yes

- name: Install mkv video tools
  ansible.builtin.apt: pkg=mkvtoolnix,mkvtoolnix-gui
  become: yes
