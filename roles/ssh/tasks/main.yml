- name: Install SSH server
  ansible.builtin.apt:
    pkg: openssh-server
  become: yes

- name: Install sshpass
  ansible.builtin.apt:
    pkg: sshpass
  become: yes

- name: Disable SSHD startup
  ansible.builtin.shell: update-rc.d ssh disable
  become: yes
