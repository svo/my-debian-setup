- name: Install nginx
  ansible.builtin.apt:
    pkg: nginx
  become: true

- name: Install SSL certificate tooling
  ansible.builtin.apt:
    pkg: certbot,python3-certbot-nginx
  become: true

- name: Install crontab
  ansible.builtin.apt:
    pkg: cron
  become: true
